// src/pages/Users.tsx
export default function Users() {
  return (
    <div className="page">
      <div className="page__header">
        <h2 className="page__title">Usuarios (Admin)</h2>
        <p className="page__subtitle">Gestión de usuarios (cuando estén los endpoints).</p>
      </div>

      <section className="card">
        <div className="card__body">
          <p className="muted">Cuando tengas endpoints de usuarios (listar/crear/editar), lo conectamos acá.</p>
        </div>
      </section>
    </div>
  );
}
